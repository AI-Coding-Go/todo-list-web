# 移动端H5待办清单应用

基于 React + TypeScript 开发的移动端待办清单应用，支持任务管理、数据统计、提醒功能等。

## 技术栈

- **框架**: React 19 + TypeScript
- **构建工具**: Vite
- **样式**: Tailwind CSS + Less
- **图标**: Font Awesome
- **图表**: Chart.js
- **时间处理**: Day.js
- **数据存储**: localStorage
- **包管理器**: pnpm

## 功能特性

### 📝 任务管理
- ✅ 创建、编辑、删除任务
- 🏷️ 优先级设置（高/中/低）
- ⏰ 截止时间设置
- 📊 任务状态管理（完成/未完成）
- 🔍 任务筛选（全部/未完成/已完成）
- 📈 任务排序（最新添加/即将截止/优先级）

### 📊 数据统计
- 📈 完成率统计（总体+按优先级）
- 📉 任务数量趋势图（近7天）
- 🥧 任务状态分布图
- 📱 响应式图表适配

### ⏰ 提醒功能
- 🔔 智能提醒（截止前30分钟、截止时间、逾期提醒）
- 🌐 浏览器桌面通知
- 🔄 稍后提醒功能
- ⚙️ 全局提醒开关

### 📱 移动端优化
- 📲 触摸友好的交互设计
- 🎨 响应式布局
- ⚡ 快速加载
- 🔄 平滑过渡动画

## 项目结构

```
src/
├── components/          # 组件
│   ├── ui/             # 通用UI组件
│   ├── task/           # 任务相关组件
│   ├── layout/         # 布局组件
│   └── reminder/       # 提醒组件
├── pages/              # 页面组件
├── hooks/              # 自定义Hooks
├── services/           # 数据服务
├── types/              # TypeScript类型定义
├── utils/              # 工具函数
├── constants/          # 常量定义
└── styles/             # 样式文件
```

## 开发指南

### 环境要求
- Node.js >= 16
- pnpm >= 7

### 安装依赖
```bash
pnpm install
```

### 开发模式
```bash
pnpm dev
```

### 构建生产版本
```bash
pnpm build
```

### 预览生产版本
```bash
pnpm preview
```

## 代码规范

### 命名规范
- 类型定义以 `I` 开头（如 `ITask`）
- 组件使用 PascalCase
- 变量和函数使用 camelCase
- 常量使用 UPPER_SNAKE_CASE

### 组件规范
- 每个文件顶部包含文件说明注释
- 单个文件不超过200行
- 组件保持单一职责
- 优先使用函数式组件和Hooks

### 样式规范
- 优先使用 Tailwind CSS
- 移动端优先设计
- 响应式布局
- 触摸友好的交互尺寸（最小44x44px）

## 浏览器支持

- Chrome >= 88
- Firefox >= 78
- Safari >= 14
- Edge >= 88

## 许可证

MIT License