# 快速启动指南

## ✅ 当前状态

- ✅ **数据库已初始化**: `todo_db` 数据库和 `todo_task` 表已创建
- ✅ **MySQL服务**: 已启动并运行正常
- ✅ **Redis服务**: 已启动并运行正常
- ✅ **配置文件**: `application.yml` 已配置完成

## 🚀 启动服务（三种方式）

### 方式一：使用IDE启动（最简单，推荐）

1. **IntelliJ IDEA**
   - 打开项目: File -> Open -> 选择项目目录
   - 等待Maven依赖自动下载
   - 找到 `src/main/java/com/todo/TodoApplication.java`
   - 右键 -> Run 'TodoApplication'
   - 或点击类名左侧的绿色运行按钮

2. **Eclipse / VS Code**
   - 导入Maven项目
   - 找到 `TodoApplication.java`
   - 右键 -> Run As -> Java Application

### 方式二：命令行启动（需要先安装Java和Maven）

#### 步骤1: 安装Java 17

```bash
# 使用Homebrew安装
brew install openjdk@17

# 配置环境变量（添加到 ~/.zshrc）
echo 'export JAVA_HOME=$(/usr/libexec/java_home -v 17)' >> ~/.zshrc
echo 'export PATH=$JAVA_HOME/bin:$PATH' >> ~/.zshrc
source ~/.zshrc

# 验证安装
java -version
```

#### 步骤2: 安装Maven

```bash
# 使用Homebrew安装
brew install maven

# 验证安装
mvn -version
```

#### 步骤3: 启动服务

```bash
cd /Users/zhouj270/Desktop/111
mvn spring-boot:run
```

### 方式三：使用启动脚本

```bash
cd /Users/zhouj270/Desktop/111
./start.sh
```

## 📋 启动后验证

服务启动成功后，你会看到类似以下输出：

```
Started TodoApplication in X.XXX seconds
```

然后可以访问：

1. **Swagger API文档**: http://localhost:8080/swagger-ui.html
2. **API文档(JSON)**: http://localhost:8080/api-docs

## 🧪 快速测试

在Swagger UI中测试，或使用curl命令：

```bash
# 创建任务
curl -X POST http://localhost:8080/api/tasks \
  -H "Content-Type: application/json" \
  -d '{
    "title": "测试任务",
    "description": "这是一个测试任务",
    "priority": 2
  }'

# 查询任务列表
curl http://localhost:8080/api/tasks
```

## ⚙️ 配置说明

当前配置（`src/main/resources/application.yml`）：
- **数据库**: localhost:3306/todo_db, 用户: root, 密码: 空
- **Redis**: localhost:6379, 密码: 空
- **服务端口**: 8080

如需修改，请编辑 `application.yml` 文件。

## ❓ 遇到问题？

1. **Java未找到**: 请安装Java 17（见方式二步骤1）
2. **Maven未找到**: 请安装Maven（见方式二步骤2）
3. **端口被占用**: 修改 `application.yml` 中的 `server.port`
4. **数据库连接失败**: 检查MySQL服务是否运行，密码是否正确

更多问题请查看 `启动说明.md`

